# Skopiuj ten plik jako `.streamlit/secrets.toml` lokalnie
# albo wklej jego zawartość w panelu Streamlit Cloud: Settings -> Secrets.
# NIE commituj `secrets.toml` do Gita (jest w .gitignore).

app_password = "ZMIEN_TO_NA_WLASNE_HASLO"
